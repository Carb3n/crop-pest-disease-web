# Pinned for Python 3.11 on Streamlit Community Cloud
streamlit==1.38.0
numpy==1.26.4
Pillow==10.4.0

# CPU-only TensorFlow — compatible with Python 3.11
# tensorflow-cpu 2.17.0 requires Python >=3.9,<3.13
tensorflow-cpu==2.17.0

ultralytics==8.2.103

# Explicit headless OpenCV pin — without this, pip resolves ultralytics'
# default opencv-python dependency, which needs libGL.so.1 (a GUI library
# not present on Streamlit Cloud's minimal container) and fails on import.
# opencv-python-headless is functionally identical for our use case (no
# GUI windows are opened) and avoids that system dependency entirely.
opencv-python-headless==4.10.0.84